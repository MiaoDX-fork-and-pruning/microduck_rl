# Historical Phase 0 jobs (retired)

These submitted job specifications are retained as evidence only. They mount
immutable source snapshots `c72423f` / `30ebb4e` containing the former shell
window loop and JSON stage-file controller. They are not launch templates for
the current code. Do not repoint these specs at HEAD.

Current training, evaluation, resume and completion use
`scripts/run_adaptive_campaign_job.py`; controller state lives in checkpoints.
See `docs/status/active/mjlab-adaptive-curriculum.md` for the active contract.
